{% extends 'base.html.twig' %}

{% block body %}
  {% block title %} Velo.tn {% endblock %}
{% if is_granted ('ROLE_USER') %}

    <div class="text-center" ><h1> Modifier reservation </h1> </div>
<br><br>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('reservation_show') }}">Retour</a>
        </li>
       
    </ul>
    {% endif %}
{% endblock %}
